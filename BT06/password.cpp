#include <bits/stdc++.h>
using namespace std;

void password(string s, int n, string p)
{
    if (n == 0)
    {
         cout << p;
         return;
    }
    
    for (int j = 0; j < s.length(); j++)
    {
        for (int i = 0; i < n; i++)
        {
            
        }
         
    }

}

int main()
{
    int n;
    cin >> n;
    string s;
    cin >> s;
    password(s, n, "");
}