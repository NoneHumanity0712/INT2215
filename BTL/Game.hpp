#ifndef Game
#define Game

#include <vector>
#include <SDL.h>
#include <SDL_ttf.h>

using namespace std;

class Game
{
private:
    static Game *mInstance;
    Game();
    SDL_Window *window;
    vector<
public:
};
